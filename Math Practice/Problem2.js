let radius = prompt("What is the radius of the cylinder in inches?"); // 6 in
let height = prompt("What is the height of the cylinder in feet?"); // 3 ft

let volume = (Math.PI * radius ** 2) * height * 12;

console.log(`The volume of the cylinder is ${volume}`);